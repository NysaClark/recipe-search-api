// import React from 'react';
// import { useGlobalContext } from "../util/context";

// const PageButtons = () => {
//     const {loading, pageNum, nbPages, changePage} = useGlobalContext();
//     return (
//         <div className="btn-container">
//             <button disabled={loading} onClick={() => changePage('-')}>prev</button>
//             <p>{pageNum} to {nbPages}</p>
//             <button disabled={loading} onClick={() => changePage('+')}>next</button>
//         </div>
//     )
// }

// export default PageButtons;

